(library
 (name mgen_corpus)
 ; [Vocab.Rtl] states the class-to-code map for the circuit, thus the corpus library
 ; elaborates. It is the one rule of the vocabulary the RTL needs, and one rule wants one
 ; definition.
 (libraries base core stdio yojson hardcaml mgen_core)
 (flags
  (:standard -open Mgen_core))
 (inline_tests
  (flags
   (:standard -source-tree-root .)))
 (preprocess
  (pps ppx_hardcaml ppx_expect ppx_compare ppx_enumerate ppx_sexp_conv)))
