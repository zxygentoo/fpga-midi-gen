(library
 (name mgen)
 ; hardcaml_waveterm and qcheck-core serve only the inline tests
 (libraries base stdio core_unix hardcaml hardcaml_waveterm qcheck-core)
 (inline_tests
  ; The ~preview ppx_expect records file basenames; the default
  ; -source-tree-root from the backend points one level too high.
  (flags
   (:standard -source-tree-root .)))
 (preprocess
  (pps ppx_hardcaml ppx_expect ppx_compare ppx_enumerate ppx_sexp_conv)))
